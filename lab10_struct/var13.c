#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct parabola{
    int a, b, c;
}


int main(){
    int M, a, b, c;
    printf("Введите кол-во парабол (M): ");
    scanf("%d", &M);
    printf("Введите параметры параболы (a, b, c): ");
    scanf("%d, %d, %d", &a, &b, &c);
}